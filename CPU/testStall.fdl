#include "src/stall.fdl"

dp teststall(out doBus : ns(1);
		out busDone : ns(1);
		in stall : ns(1))
{
	always
	{
		doBus = 1;
		busDone = 0;
		$display($dec, stall);
	}
}

system sda
{
	shouldStall(doBus, busDone, stall);
	teststall(doBus, busDone, stall);
}
