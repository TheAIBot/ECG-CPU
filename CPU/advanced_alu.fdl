dp alu(in a, b : ns(32); 
	   in select : ns(2); 
	   out ALUout : ns(32)) {
	always {
		ALUout = select == 0b00 ? a + b :
				 select == 0b01 ? a - b :
				 select == 0b10 ? a >> b :
					 		 	  a & b;
	}
}
