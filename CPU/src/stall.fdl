dp shouldStall(in doBus : ns(1);
			   in busDone : ns(1);
			   in useBus : ns(1);
			   in memoryStall : ns(1);
			   out stall : ns(1))
{
	always
	{
		stall = (doBus & (~busDone) & useBus) | memoryStall;
	}
}
